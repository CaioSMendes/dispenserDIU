<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= form_with(model: seller, class: "form") do |form| %>
        <% if seller.errors.any? %>
          <div class="alert alert-danger">
            <h2><%= pluralize(seller.errors.count, "error") %> prohibited this seller from being saved:</h2>

            <ul>
              <% seller.errors.each do |error| %>
                <li><%= error.full_message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :status %>
          <%= form.text_field :status, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :nome %>
          <%= form.text_field :nome, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :cardRFID %>
          <%= form.text_field :cardRFID, class: "form-control" %>
        </div>

        <div class="form-group">
          <%= form.label :cargo %>
          <%= form.text_field :cargo, class: "form-control" %>
        </div>

        <div class="form-group mb-4">
          <%= form.label :contador %>
          <%= form.number_field :contador, class: "form-control" %>
        </div>
        <div class="button-group">
          <div class="form-group d-inline-block">
            <%= form.submit "Cadastrar Usuário", class: "btn btn-primary" %>
          </div>
          <div class="form-group d-inline-block">
            <%= link_to "Voltar", sellers_path, class: "btn btn-success" %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
